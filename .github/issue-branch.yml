mode: auto

autoLinkIssue: true
autoCloseIssue: true

defaultBranch: 'develop'
branchName: '${issue.number}-${issue.title}'
branches:
  - label: enhancement
    prefix: feature/
  - label: bug
    prefix: fix/
  - label : '*'
    prefix: issue/
